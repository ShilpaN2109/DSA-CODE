#Given an array A[] of size n. The task is to find the largest element in it.

def largest( arr, n):
    mx = arr[0]
    for i in range (1, n):
        if arr[i]>mx:
            mx = arr[i]
    return mx
        
